language: node_js

node_js:
  - "4"

os:
- linux
- osx

notifications:
  email:
    on_success: never
    on_failure: change

before_install:
  - git submodule sync && git submodule update --init --recursive

branches:
  only:
    - master
